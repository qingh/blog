/** @type {import('next').NextConfig} */

module.exports = {
  reactStrictMode: true,
  // images:{domains:['localhost:8888'],formats: ['image/avif', 'image/webp'],}
  /* rewrites() {
    return [
      {
        source: '/api/v1:path*', destination: `http://127.0.0.1:8888/:path*`
      }
    ]
  } */
}
